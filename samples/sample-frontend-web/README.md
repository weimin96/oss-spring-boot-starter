# 前端示例

该模块用于演示如何通过浏览器调用后端 sample 暴露的 OSS 接口。

## 启动方式

进入当前目录后执行：

```powershell
npm install
npm run dev
```

## 环境变量

复制 `.env.example` 为 `.env`，并按实际后端地址调整：

```text
VITE_API_BASE_URL=http://127.0.0.1:8080/api
```

## 当前能力

- 